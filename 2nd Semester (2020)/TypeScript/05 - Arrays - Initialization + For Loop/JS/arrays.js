// Initialization of String Arrays in JS

    var ar1 = ["Numbers", "Strings", "Booleans", "NULL"];

    console.log("\n String Array :\n");
    for (var a = 0; a < ar1.length; a++) {
        console.log(ar1[a]);
    }

// Initialization of Number Arrays in JS

    var ar2 = [17, 9, 78, 1, 9, 0, 4, 71, 8, 27, 5, 11];

    console.log("\n Number Array :\n");
    for (var a = 0; a < ar2.length; a++) {
        console.log(ar2[a]);
    }

// Initialization of Boolean Arrays in JS (Optional)

    var ar3 = [true, true, true, false, false, false];

    console.log("\n Boolean Array :\n");
    for (var a = 0; a < ar3.length; a++) {
        console.log(ar3[a]);
    }
